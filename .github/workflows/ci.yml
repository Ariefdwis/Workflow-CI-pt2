name: CI/CD Pipeline for Model

on:
  push:
    branches: [ "main" ]

jobs:
  train_model:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          # Menambahkan library dagshub agar bisa connect ke remote
          pip install pandas scikit-learn mlflow dagshub

      - name: Run Model Training (Retraining with MLflow)
        env:
          MLFLOW_TRACKING_URI: ${{ secrets.MLFLOW_TRACKING_URI }}
          MLFLOW_TRACKING_USERNAME: ${{ secrets.MLFLOW_TRACKING_USERNAME }}
          MLFLOW_TRACKING_PASSWORD: ${{ secrets.MLFLOW_TRACKING_PASSWORD }}
        run: |
          cd MLProject
          # Pastikan file modelling.py di folder ini ISINYA adalah script yang Advanced (yang connect ke DagsHub)
          python modelling.py

  build_push_docker:
    needs: train_model
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v4
        with:
          context: ./MLProject
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/water-potability-model:latest
